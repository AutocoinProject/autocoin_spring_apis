# ===========================================
# 테스트 환경 기본 설정
# ===========================================
spring:
  profiles:
    active: test
    
# 기본 로깅 레벨
logging:
  level:
    root: WARN
    com.autocoin: INFO
